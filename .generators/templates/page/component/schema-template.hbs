import { z } from 'zod';

export const {{pascalCase moduleName}}FormSchema = z.object({
  fullname: z.string().min(1),
  password: z.string().min(1),
  email: z.string().email(),
  address: z.string().min(1),
  roleId: z.number(),
});

export type {{pascalCase moduleName}}FormData = z.infer<typeof {{pascalCase moduleName}}FormSchema>;
