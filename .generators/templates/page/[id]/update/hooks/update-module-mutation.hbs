import { TUpdate{{pascalCase moduleName}}Payload, update{{pascalCase moduleName}} } from '@/api/{{dashCase moduleName}}';
import { useMutation } from '@/app/_hooks/request/use-mutation';

export const useUpdate{{pascalCase moduleName}}Mutation = (id: string) => {
  return useMutation({
    mutationKey: ['update-{{dashCase moduleName}}', id],
    mutationFn: (data: TUpdate{{pascalCase moduleName}}Payload) => update{{pascalCase moduleName}}(id, data),
  });
};
