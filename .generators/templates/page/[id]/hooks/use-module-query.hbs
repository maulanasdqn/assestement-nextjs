import { get{{pascalCase moduleName}} } from '@/api/{{dashCase moduleName}}';
import { useQuery } from '@/app/_hooks/request/use-query';

export const use{{pascalCase moduleName}}Query = (id: string) => {
  return useQuery({
    queryKey: ['{{dashCase moduleName}}', id],
    queryFn: () => get{{pascalCase moduleName}}(id),
  });
};
